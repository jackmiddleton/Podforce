<div class="dashboard-container">
  <div class="row">
    <div class="sidebar col-2 col-sm-1">
      <%= render 'shared/sidebar' %>
    </div>

    <div class="col-10">
      <div class="row inner-navbar">
        <%= render 'shared/navbar' %>
      </div>

      <div class="container">
        <div class="row">
          <div class="col-12">
            <span class="secbtn"><%= link_to "New Task", new_task_path %></span>
            <span class="secbtn"><%= link_to "Back", tasks_path %></span>
            <div class="row">
              <div class="col-12 p-2">
                <div class="card-task">
                  <h2><%= @task.description %></h2>
                  <p class="card-text"> <strong>Priority: </strong><%= @task.priority %></p>
                  <p class="card-text"> <strong>Date: </strong><%= @task.date_time %></p>
                  <p class="card-text"> <strong>Complete: </strong><i class="far <%= @task.complete ? "fa-check-square" : "fa-square" %>"></i></p>

                  <p class="card-text"> <strong>Category: </strong><%= @task.category %></p>

                  <% if @task.category == "email" && @task.contact.present? %>
                    <div>
                      <a class= "secbtn" href="mailto: <%= @task.contact.email %> ?subject=Episode Live" style="">Send Email</a>
                    </div>
                  <%end%>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
